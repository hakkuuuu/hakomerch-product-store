version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - MONGO_URI=${MONGO_URI}
      - UPLOADS_DIR=uploads/
    volumes:
      - ./backend/uploads:/app/backend/uploads
    restart: unless-stopped 